# Copy to .env and fill in real values before running `docker-compose up`.
# .env is git-ignored - never commit real secrets.

# JWT signing key (HS256, needs >= 256 bits). Generate with: openssl rand -base64 32
APP_JWT_SECRET=
